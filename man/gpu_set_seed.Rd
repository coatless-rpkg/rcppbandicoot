% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{gpu_set_seed}
\alias{gpu_set_seed}
\title{Set the GPU random number generator seed}
\usage{
gpu_set_seed(seed)
}
\arguments{
\item{seed}{Seed for the device generator: a non-negative whole number no
larger than \code{2^53}. R has no unsigned 64-bit type, so the seed arrives as
a double and is converted to the 64-bit unsigned value Bandicoot wants;
\code{2^53} is the largest integer a double still represents exactly.}
}
\value{
Invisibly \code{NULL}. Called for its side effect on the device
generator.
}
\description{
Seeds the device-side generator that backs \code{\link[=gpu_randu]{gpu_randu()}}. Bandicoot
defaults that seed to \code{0} and never randomises it, so without an explicit
seed every fresh process would draw the same numbers.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("RCPPBANDICOOT_RUN_GPU_EXAMPLES"))) withAutoprint(\{ # examplesIf}
gpu_set_seed(42)
gpu_randu(2, 2)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=gpu_randu]{gpu_randu()}}, which seeds the device from R's own random number
stream on the first draw of a session when this has not been called.
}
